# Skim autoreload
defaults write -app Skim SKAutoReloadFileUpdate -boolean true
# umlaut press and hold
defaults write -g ApplePressAndHoldEnabled -bool true
# defaults write -app "Sublime Text" -bool false
# defaults write -app "Terminal" -bool false
# defaults write -app "Visual Studio Code" -bool false
# defaults write -app "Setapp/CodeRunner" -bool false